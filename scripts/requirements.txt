# Known-good pin set (Python 3.12, Apple Silicon). Deliberately violates mlx-audio's declared
# constraints (transformers 5.x breaks marker-pdf; huggingface_hub 1.x untested here), so
# setup.sh installs mlx-lm, mlx-audio, and nanocodec-mlx with --no-deps on top of this set.
mlx==0.32.0
marker-pdf==1.10.2
surya-ocr==0.17.1
kokoro==0.9.4
misaki==0.9.4
soundfile==0.13.1
numpy==1.26.4
torch==2.10.0
transformers==4.57.6
regex==2024.11.6
FlagEmbedding==1.4.0
huggingface_hub==0.36.2
# mlx-audio/mlx-lm runtime deps, installed here since --no-deps skips them
miniaudio==1.71
scipy==1.17.0
sounddevice==0.5.5
sentencepiece==0.2.1
tqdm==4.67.3
protobuf==7.34.0
PyYAML==6.0.3
Jinja2==3.1.6
