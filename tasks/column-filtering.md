# Column Filtering

For multi-column PDFs (e.g., parallel translations), filter blocks by x-coordinate to keep only the left or right column.

Marker JSON preserves `bbox`/`polygon` coordinates per block. The raw JSON is kept in `data/tmp/{bookId}/` for this purpose.
